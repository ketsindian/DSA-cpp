#include <bits/stdc++.h>
using namespace std;
typedef int ll;
typedef vector<ll> VLL;
typedef set<ll> SLL;
typedef pair<ll, ll> PLL;
#define lim 10000007
#define mod 1000000007

int main(){
    ll t;
    cin>>t;
    while(t--){
        ll r,g,b;
        cin>>r;
        cin>>g;
        cin>>b;
        vector<ll> v;
        v.push_back(r/3);
        v.push_back(g/3);
        v.push_back(b/3);
        sort(v.begin(),v.end());
        if(v[0]==1&&v[1]==1&&v[2]==[1]){
            
        }
    }
}